
/**     @module infosex
    **INFO**rmation **S**ciences **EX**change format package. Creates and parses standard strings
    for a constellation of interoperable webapps.
*/

module.exports.uid      = require ('./lib/uid');
module.exports.session  = require ('./lib/session');
